# tools-config
## vim
1. `cp -r vim/autoload ~/.vim`
2. `cp vim/.vimrc ~/.vimrc`

## vscode
1. `cp vscode/ ~/vscode/`

## git
1. `cp git/.gitconfig ~/.gitconfig`

## bashrc
1. `cp bash/.bashrc ~/.bashrc`
