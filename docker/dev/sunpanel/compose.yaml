services:
  sun-panel:
    image: hslr/sun-panel:2.0.1-dev-latest
    restart: always
    ports:
      - "5555:3002"
    volumes:
      - ./conf:/app/conf
      - /var/run/docker.sock:/var/run/docker.sock:ro  # 可选
    environment:
      - TZ=Asia/Shanghai
